# cargo-audit configuration
# Suppress known transitive advisories that cannot be fixed without upstream changes.
# Reviewed: 2026-03-29

[advisories]

# RUSTSEC-2023-0089: atomic-polyfill is unmaintained
# Path: postcard 1.1.x → heapless 0.7.x → atomic-polyfill
# Fix: waiting for postcard to upgrade heapless dependency to 0.8.x
ignore = [
    "RUSTSEC-2023-0089",

    # RUSTSEC-2024-0436: paste is unmaintained
    # Path: libp2p 0.55 → if-watch → netlink-packet-core → paste
    # Fix: waiting for libp2p 0.56+
    "RUSTSEC-2024-0436",

    # RUSTSEC-2026-0002: lru IterMut unsoundness (Stacked Borrows)
    # Path: libp2p-swarm 0.46 → lru 0.12.5
    # Fix: waiting for libp2p 0.56+ which pins lru 0.13+
    "RUSTSEC-2026-0002",
]
